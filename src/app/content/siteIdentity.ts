export const SITE_NAME = "Gorf";
export const SITE_LOCATION = "Toronto, Canada";

/** Short role line used in UI labels where space is tight. */
export const SITE_ROLE_SHORT = "Producer, Composer & Live PA";

/** Full human-facing tagline used in footer and social labels. */
export const SITE_ROLE_TAGLINE = `${SITE_ROLE_SHORT} from ${SITE_LOCATION}.`;
export const SITE_ROLE_TAGLINE_NO_PERIOD = `${SITE_ROLE_SHORT} from ${SITE_LOCATION}`;

/** Home-page title line requested by brand direction. */
export const HOME_META_TITLE = `${SITE_NAME} | Electronic Music Producer, Composer & Live PA`;

/** Shared DOM id: used to sync compact header wordmark visibility with the home hero wordmark. */
export const HOME_HERO_WORDMARK_ID = "home-hero-wordmark";
